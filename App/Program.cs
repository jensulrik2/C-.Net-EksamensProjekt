using DataAccess.Context;
using System;

internal class Program
{
    static void Main(string[] args)
    {

        /* using (var context = new Context())
        {
            // The database will be created when the context is instantiated
            Console.WriteLine("Database creation triggered.");
        }
        */
    }
}